name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - readme
    paths:
      - .github/workflows/ci.yml

env:
  SCRIPT_REPO: ${{ github.repository }}
  SCRIPT_REF: ${{ github.ref_name }}
  # 使用已知可用的U-Boot仓库
  UBOOT_REPO: armbian/u-boot
  UBOOT_TAG: v2023.10  # 使用固定版本
  UBOOT_BURNIMG: u-boot.bin
  ARMBIAN_REPO: armbian/build
  ARMBIAN_REF: main
  PATCHES: 4077,5076
  PATCHES_DISABLED: 5082
  # 添加必要的内核参数
  KERNEL_MAJOR_MINOR: "6.1"
  LINUXFAMILY: "meson8b"
  # 镜像构建参数
  COMPRESS_OUTPUTIMAGE: "img"
  FORCE_BINARIES: "yes"

jobs:
  prepare:
    name: Prepare
    runs-on: ubuntu-22.04
    outputs:
      env: ${{ steps.get.outputs.env }}
    steps:
      - id: get
        name: Get repositories information
        run: |
          TIME="$(date -u +'%Y-%m-%dT%H:%M:%SZ')"
          TAG="ci-$(date -d "${TIME}" -u +'%Y%m%d-%H%M%S-%Z')"
          
          # 直接使用预定义的UBOOT_TAG，避免API调用失败
          SCRIPT_SHA=$(curl -s https://api.github.com/repos/${SCRIPT_REPO}/commits/${SCRIPT_REF} | jq -r .sha || echo ${{ github.sha }})
          ARMBIAN_SHA=$(curl -s https://api.github.com/repos/${ARMBIAN_REPO}/commits/${ARMBIAN_REF} | jq -r .sha)

          cat <<EOF | sed -E 's/^  //' >>$GITHUB_ENV
            TIME=$TIME
            TAG=$TAG
            SCRIPT_SHA=$SCRIPT_SHA
            UBOOT_TAG=$UBOOT_TAG
            ARMBIAN_SHA=$ARMBIAN_SHA
          EOF
          echo "env=$(cat $GITHUB_ENV | base64 -w 0)" >>$GITHUB_OUTPUT

  build-debs:
    name: Build ${{ matrix.BRANCH }}
    needs: prepare
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        BRANCH: [current]

    steps:
      - name: Import env
        run: |
          echo "$(echo "${{ needs.prepare.outputs.env }}" | base64 -d)" >>$GITHUB_ENV

      - name: Checkout Armbian
        uses: actions/checkout@v4
        with:
          repository: ${{ env.ARMBIAN_REPO }}
          ref: ${{ env.ARMBIAN_SHA }}

      - name: Prepare build environment
        run: |
          sudo apt-get update && sudo apt-get install -y \
            jq curl img2simg parted udev \
            gcc make bc device-tree-compiler \
            u-boot-tools flex bison libssl-dev \
            python3-dev swig python3-distutils
            
          mkdir -p config/kernel/ userpatches/kernel/archive/meson8b-6.1/
          
          # 下载内核配置
          if [ ! -f "config/kernel/linux-meson8b-current.config" ]; then
            curl -L -o config/kernel/linux-meson8b-current.config \
              https://raw.githubusercontent.com/hzyitc/armbian-onecloud/main/config/kernel/linux-meson8b-current.config || \
            echo "CONFIG_ARM=y" > config/kernel/linux-meson8b-current.config
          fi

      - name: Apply patches
        if: ${{ env.PATCHES }}
        run: |
          for id in $(echo ${{ env.PATCHES }} | tr ',' ' '); do
            curl -L -O "https://github.com/armbian/build/pull/${id}.patch" || echo "Failed to download patch ${id}, continuing..."
            patch --batch -p1 -N <"${id}.patch" || echo "Patch ${id}.patch failed, continuing..."
          done

      - name: Build kernel packages
        run: |
          export GITHUB_ACTIONS=false
          sudo --preserve-env \
            ./compile.sh kernel \
              ALLOW_ROOT=yes \
              BOARD=onecloud \
              BRANCH=${{ matrix.BRANCH }} \
              EXPERT=yes \
              USE_CCACHE=no \
              KERNEL_MAJOR_MINOR=${{ env.KERNEL_MAJOR_MINOR }} \
              LINUXFAMILY=${{ env.LINUXFAMILY }}
          sudo chown -R $(id -u):$(id -g) output/

      - name: Upload debs
        uses: actions/upload-artifact@v4
        with:
          name: debs-${{ matrix.BRANCH }}
          path: output/debs/*

  build:
    name: Build ${{ matrix.RELEASE }}-${{ matrix.BRANCH }}-${{ matrix.TYPE }}
    needs: [prepare, build-debs]
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        BRANCH: [current]
        RELEASE: [jammy, bullseye, bookworm]
        TYPE: [minimal, cli, desktop]
        include:
          - TYPE: desktop
            DESKTOP_ENVIRONMENT: xfce
            DESKTOP_ENVIRONMENT_CONFIG_NAME: config_base
            DESKTOP_APPGROUPS_SELECTED: ""

    steps:
      - name: Import env
        run: |
          echo "$(echo "${{ needs.prepare.outputs.env }}" | base64 -d)" >>$GITHUB_ENV

      - name: Checkout Armbian
        uses: actions/checkout@v4
        with:
          repository: ${{ env.ARMBIAN_REPO }}
          ref: ${{ env.ARMBIAN_SHA }}

      - name: Download kernel packages
        uses: actions/download-artifact@v4
        with:
          name: debs-${{ matrix.BRANCH }}
          path: output/debs/

      - name: Build system image
        run: |
          set -x
          # 确保输出目录存在
          mkdir -p output/images/
          
          # 构建镜像（添加详细日志和必要参数）
          export GITHUB_ACTIONS=false
          sudo --preserve-env \
            ./compile.sh build \
              ALLOW_ROOT=yes \
              BOARD=onecloud \
              BRANCH=${{ matrix.BRANCH }} \
              RELEASE=${{ matrix.RELEASE }} \
              KERNEL_CONFIGURE=no \
              BUILD_MINIMAL=${{ matrix.TYPE == 'minimal' && 'yes' || 'no' }} \
              BUILD_DESKTOP=${{ matrix.TYPE == 'desktop' && 'yes' || 'no' }} \
              DESKTOP_ENVIRONMENT=${{ matrix.DESKTOP_ENVIRONMENT }} \
              DESKTOP_ENVIRONMENT_CONFIG_NAME=${{ matrix.DESKTOP_ENVIRONMENT_CONFIG_NAME }} \
              DESKTOP_APPGROUPS_SELECTED=${{ matrix.DESKTOP_APPGROUPS_SELECTED }} \
              EXPERT=yes \
              SKIP_EXTERNAL_TOOLCHAINS=yes \
              CLEAN_LEVEL=none \
              USE_CCACHE=no \
              COMPRESS_OUTPUTIMAGE=${{ env.COMPRESS_OUTPUTIMAGE }} \
              FORCE_BINARIES=${{ env.FORCE_BINARIES }} \
              KERNEL_MAJOR_MINOR=${{ env.KERNEL_MAJOR_MINOR }} \
              LINUXFAMILY=${{ env.LINUXFAMILY }}
              
          # 验证镜像生成
          if [ -z "$(ls output/images/*.img 2>/dev/null)" ]; then
            echo "Error: No image files generated!"
            echo "Build log tail:"
            tail -n 100 output/debug/compile.log || true
            exit 1
          fi
          
          sudo chown -R $(id -u):$(id -g) output/

      - name: Install dependencies for image processing
        run: |
          sudo apt install -y img2simg parted
          curl -L -o ./AmlImg \
            https://github.com/hzyitc/AmlImg/releases/download/v0.3.2/AmlImg_v0.3.2_linux_amd64
          chmod +x ./AmlImg

      - name: Process non-burning images
        run: |
          # 处理普通镜像
          for img in output/images/*.img; do
            if [[ "$img" != *".burn.img" ]]; then
              echo "Processing regular image: $img"
              sha256sum "$img" >> sha256sum
              xz --threads=0 --compress "$img"
            fi
          done

      - name: Process burning version
        run: |
          set -e
          # 确保有镜像文件生成
          diskimg=$(ls output/images/*.img 2>/dev/null | grep -v ".burn.img" | head -1)
          if [ -z "$diskimg" ]; then
            echo "Error: No base image found for burning version"
            ls -la output/images/ || true
            exit 1
          fi
          
          echo "Processing burning version for: $diskimg"
          
          # 准备烧录镜像目录
          mkdir -p burn/
          
          # 下载U-Boot（使用预定义的URL）
          curl -L -o ./uboot.img \
            https://github.com/${UBOOT_REPO}/releases/download/${UBOOT_TAG}/${UBOOT_BURNIMG} || {
            echo "Failed to download U-Boot"
            exit 1
          }
          
          # 处理系统镜像
          echo "Setting up loop device for $diskimg"
          loop=$(sudo losetup --find --show --partscan "$diskimg")
          echo "Loop device: $loop"
          
          echo "Creating sparse images..."
          sudo img2simg ${loop}p1 burn/boot.simg || {
            echo "Failed to create boot.simg"
            sudo losetup -d $loop
            exit 1
          }
          sudo img2simg ${loop}p2 burn/rootfs.simg || {
            echo "Failed to create rootfs.simg"
            sudo losetup -d $loop
            exit 1
          }
          sudo losetup -d $loop
          
          # 生成校验文件
          echo "Generating verification files..."
          echo -n "sha1sum $(sha1sum burn/boot.simg | awk '{print $1}')" > burn/boot.VERIFY
          echo -n "sha1sum $(sha1sum burn/rootfs.simg | awk '{print $1}')" > burn/rootfs.VERIFY
          
          # 创建烧录命令文件
          cat <<EOF > burn/commands.txt
          PARTITION:boot:sparse:boot.simg
          VERIFY:boot:normal:boot.VERIFY
          PARTITION:rootfs:sparse:rootfs.simg
          VERIFY:rootfs:normal:rootfs.VERIFY
          EOF
          
          # 生成XML配置文件
          cat <<EOF > burn/package.xml
          <?xml version="1.0" encoding="utf-8"?>
          <package>
            <head>
              <product>OneCloud</product>
              <version>1.0</version>
            </head>
            <body>
              <command name="PARTITION" type="boot" filetype="sparse" filename="boot.simg"/>
              <command name="VERIFY" type="boot" verifytype="normal" filename="boot.VERIFY"/>
              <command name="PARTITION" type="rootfs" filetype="sparse" filename="rootfs.simg"/>
              <command name="VERIFY" type="rootfs" verifytype="normal" filename="rootfs.VERIFY"/>
            </body>
          </package>
          EOF
          
          # 生成烧录镜像
          prefix=$(basename "$diskimg" .img)
          ./AmlImg pack "output/images/${prefix}.burn.img" burn/package.xml || {
            echo "Failed to generate burn image"
            exit 1
          }
          
          # 处理烧录镜像
          sha256sum "output/images/${prefix}.burn.img" >> sha256sum
          xz --threads=0 --compress "output/images/${prefix}.burn.img"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: images-${{ matrix.RELEASE }}-${{ matrix.TYPE }}
          path: |
            output/images/*.img.xz
            sha256sum
